{% extends 'base.html.twig' %}

{% block title 'Edit Pin ' ~ pin.id %}

{% block body %}
    <div class="row justify-content-md-center">
        <div class="col-md-6">
            <h1>Edit Pin #{{ pin.id }}</h1>

            {{ include('pin/_form.html.twig', {submitButtonText: 'zapisz zaminy'}) }}
        </div>
    </div>

{% endblock %}