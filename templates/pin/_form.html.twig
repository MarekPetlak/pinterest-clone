{{ form_start(pinForm) }}
    {{ form_widget(pinForm) }}

    <div class="form-group">
        <input type="submit" value="{{ submitButtonText|default('zapisz') }}" class="btn btn-primary">
    </div>
{{ form_end(pinForm) }}