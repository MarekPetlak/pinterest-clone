{% extends 'base.html.twig' %}

{% block title pin.title %}

{% block body %}
<article>
    <h1>{{ pin.title }}</h1>
    <p>{{ pin.description }}</p>
</article>
    <nav><a href="{{ path('homepage') }}">Return to Homepage</a></nav>
{% endblock %}
